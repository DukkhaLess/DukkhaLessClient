language: node_js
dist: trusty
sudo: required
node_js: stable
addons:
  apt:
    packages:
      - docker-ce
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - npm run -s build
  - npm run -s test
  - sh docker_deploy.sh
after_script:
  - docker logout
